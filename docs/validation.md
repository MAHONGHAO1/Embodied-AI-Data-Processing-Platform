# v0.1 初版验证记录（规则 0.1.1）

验证日期：2026-09-10。环境：Windows、Python 3.12.10、CPU；依赖见 uv.lock。应用 v0.1.0，检查规则 0.1.1。

本文件及配套 [validation.json](validation.json) 保存初版验收快照，包括当时的 38 项测试结果。规则 0.1.2 阶段新增的故障演示、中文错误和 64 项测试结果见 [后续验证记录](failure_validation.md)。两个阶段的测试数量与结果摘要各自对应其记录版本。

## 真实样本

- 固定来源：jmrog/so100_sweet_pick @ 54141acb0bd6bcb868e34b4eb328f26481d333b2。
- 15 个文件逐一通过上游 Git blob SHA-1 / LFS SHA-256 校验；合计 53,108,400 字节（含源元数据）。
- 任务 0–4 实际读取行数：503、797、497、898、867，总计 3,562。
- 5 个 AV1 视频完整解码成功，解码帧数分别与表行数一致，图像 640×480，使用 libdav1d。
- 已查看每条任务首、中、末共 15 张图像：显示真实桌面环境、蓝色机械臂、糖果容器及不同操作阶段；机器人具体型号依据上游元数据。
- 15 个定位点均可读取且与目标记录时间对应。样本时间与 frame_index / 30 的最大差约 0.89 微秒，仅反映名义采样与浮点精度，不是硬件同步精度。
- 原始样本 55/55 项规则检查已执行：0 条数据异常、0 条读取失败、0 条未检查说明。当前输入与本地下载清单一致。
- 同输入同参数重复运行，结果摘要一致：b621d4042dc33315d299e19ca38912581409c7af46ba5224ebc052d2e720cddd。

## 测试与页面

- 初版全套 pytest：38 passed in 9.62s。包括 10 项来源／读取／视频测试，25 项质检／报告测试，3 项 Streamlit AppTest。
- 已覆盖：缺字段、NaN/Inf、维度错误、帧索引异常、时间倒退、异常间隔、任务间正常归零、缺视频、损坏视频、来源变动、参数变更、异常定位跳转、HTML 转义和 JSON 一致性。
- 回归用例：整条时间偏移 +5 ms 时，末帧超出视频可读范围必须报告；1 微秒级浮点偏差仍能通过；负时间明确报告。
- 内置浏览器实际操作：打开概览、运行检查、浏览任务中间帧及曲线、打开报告，点击 HTML 与 JSON 下载按钮，两个下载事件均成功。
- 启动入口已通过 Windows PowerShell 5.1 实际启动；页面在 http://127.0.0.1:8501 运行。启动脚本带 UTF-8 BOM，避免系统 PowerShell 将中文脚本按本地编码误读。

## 性能口径与限制

该验证环境中，一次独立命令行全量检查约 2.23 秒，仅为这 5 条样本的单次观测，不代表大规模吞吐。连续执行可能复用已解码的时间索引；页面显示结果生成时间，缓存命中耗时不作为重新处理数据的性能指标。

该记录未验证：动作物理单位及控制模式、硬件时钟同步、双机位同步、任务成功率或训练适用性。该阶段未实现训练格式转换或模型训练，服务在本机运行。


## 后续验证记录

中文错误提示、可切换失败副本与页面实际下载验证见 [失败流程验证](failure_validation.md)。原始样本复查仍为 55/55 项、0 条问题。
