# V0.2 阶段验收

2026-09-10，本机 Windows / Python 3.12 / CPU。进入真实 HDF5 转换开发前完成。

- 全套测试：73 passed（123.02 秒）；后续 UI 小修后的 5 项 AppTest 再次通过。
- `scripts/verify_runs.py`：真实原始样本、重复运行、异常副本、缺视频、执行超时五种场景通过，证据在 `work/v02-acceptance.json`。
- 原始 5 条 / 3562 帧 / 55 项检查；两次运行编号不同、结果摘要一致；原始文件指纹未变。
- 异常副本 7 条问题；缺视频 1 条读取失败 / 2 条未检查 / 52 项已执行；超时终止任务 0 视频进程并继续其他 4 条，表格结果保留。
- 真实进程测试覆盖子进程退出、协调器被终止、单活动锁竞争、报告写入失败、元数据损坏后历史可读。
- 浏览器实际点击运行基础质检、刷新恢复同一编号 `20260910T153925_44f7bc5322a2`、查看 55/55 报告、按任务筛选 9/53 条日志、点击 JSONL / HTML / JSON 下载按钮。AppTest 覆盖三级日志筛选、异常行跳转和输入失效后历史恢复。
- HTML 内嵌 JSON 与独立报告一致。报告节点状态已修正，完成产物不再显示永久运行中。

后续阶段：在独立环境实施真实 HDF5 到 LeRobot v3.0 转换；应用版本和数据格式版本分别展示。
