# V0.4.0 实际验收记录

验收日期：2026-09-11。环境为 Windows、Python 3.12、CPU、本地单用户。应用版本 `0.4.0`；基础规则版本保持 `0.3.0`；输出格式为 LeRobotDataset `v3.0`。

## 真实八节点闭环

使用固定 `robomimic/robomimic_datasets` revision `74fa018461f479cd9fd15b924a16103012096203` 的 `test/test.hdf5`。源文件 SHA-256 为 `80dea9ca9bb99dd7b96109712fc6b46bb6fa6b87e9622b8aebbe1e4ee5fadbab`，原文件在链路后保持不变。

最新自动运行 `20260910T173026_631fc1c917c8`：导入 3 条／174 帧，筛选保留源任务 0、2，最终输出 2 条／116 帧；最终质检覆盖 22/22 项且无异常。官方转换验证检查全量 116 帧数值、每任务首中末共 6 个样本及 `DataLoader(batch_size=2,num_workers=0)`；输出视频第二条任务偏移 2.95 秒，工作台和官方读取器定位一致。随后生成 ZIP，独立解包验证和官方离线加载通过。

运行记录、阶段 JSON、JSONL 日志、最终质检 HTML／JSON、转换产物、交付 ZIP 和登记哈希均保存在 `work/runs/20260910T173026_631fc1c917c8/`；汇总证据为 `work/eight-node-release-acceptance.json`。ZIP 的整包 SHA-256 为 `50bc52b964116b639428324e5a446a3ad12b88c03a8fc02865b89bcda204be75`。

## 故障与门禁

- HDF5 隔离副本：任务 0 的 `actions` 注入 NaN、任务 1 缺少 `obs/robot0_eef_pos`，两条被隔离，任务 2 的 57 帧可继续转换；原始源文件未修改。
- SO-100 异常副本：7 条问题可定位；缺视频副本记录 1 条读取失败和 2 条未检查，其余任务继续。
- 篡改交付 ZIP：清单／哈希验证节点拒绝，官方加载不启动。
- 自动链不通过时保存诊断和报告，转换、打包保持跳过；报告、源管理副本或最终 HTML 校验值变化后，当前产物下载和下游门禁关闭，旧结果标为历史。

## 测试与页面

最终全套主环境测试：**179 passed，13 subtests passed，252.95 秒**，JUnit 与控制台分别保存在 `work/v04-tests.xml` 和 `work/v04-tests.log`。针对最新运行状态、产物有效性和业务页面的定向测试也包含在该套测试中。

浏览器实际检查 `http://127.0.0.1:8501/`：侧栏和流程总览均显示八个节点；逐页进入八个节点及运行记录；格式转换页实际下载验证证据 JSON，打包交付页实际下载当前 ZIP；页面显示 0.4.0、LeRobot v3.0、116 帧和历史包过期提示。实际浏览器下载文件保存在本机 Downloads，未上传到外部服务。

## 边界

本版本验证固定数据配置和一条本机 CPU 转换链路，不宣称支持任意 HDF5、全部 LeRobot 版本、MCAP、多相机硬件同步、自动轨迹修复、模型训练或 GitHub 远程发布。人工标注的失败只表示流程中保留了失败标签，不推断公开仿真片段的真实机器人成功率。

复现入口见 [SOP](SOP.md)、[演示路线](demo.md) 和 `scripts/verify_flow.py`；原始数据、工作记录和报告默认被 Git 忽略。
